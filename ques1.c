//printing the error if number is greater than 100 and accepting it if it is less than 100 
#include <stdio.h>

int main()
{
    int num;
    printf("enter the value of num : ");
    scanf("%d",&num);
    if (num<100)
    printf("number accepted");
    else
    printf("error");
    retuen o;

}
